#include<bits/stdc++.h>
using namespace std;
//Unique Numbers
//Brute will be linear search we iwll compre each and every number with the key value and the then return the index.
//Time Complexity will be O(N)

int searchInRotatedrray(int arr[] , int n , int x){
    
}

int main(){
    int arr[]={7,8,9,1,2,3,4,5,6};
    int n = sizeof(arr)/sizeof(arr[0]);

    int key;
    cin>>key;
    
    int index = searchInRotatedrray(arr, n, key); //for iterative
    return 0;
}